# 파이썬 데이터 분석 시험문제 (자동채점용)

1. pandas 라이브러리를 불러올 때 관습적으로 사용하는 별명(alias)은? (2점)
- pa
* pd
- pn
- pds
> import pandas as pd — 전 세계 분석가들의 관습적 약속.

2. Series에 대한 설명으로 옳은 것은? (2점)
- 행과 열로 이루어진 2차원 표 구조이다
* 인덱스와 값의 쌍으로 이루어진 1차원 데이터 구조이다
- 반드시 숫자 인덱스(0,1,2…)만 사용해야 한다
- 여러 개의 DataFrame을 모아 놓은 구조이다
> Series는 엑셀의 '한 열'에 해당하는 인덱스+값의 1차원 구조이다.

3. DataFrame에 대한 설명으로 가장 적절한 것은? (2점)
- 값 하나만 저장하는 변수이다
- 1차원 구조로 한 줄의 데이터만 담는다
* 여러 Series(열)를 옆으로 나란히 붙여 놓은 2차원 표이다
- 그래프를 그리는 전용 객체이다
> DataFrame은 여러 Series를 모은 2차원 표(엑셀 시트 한 장)이다.

4. 다음 코드에서 빈칸에 들어갈 속성으로, DataFrame의 (행 수, 열 수)를 튜플로 반환하는 것은? (2점)
```python
print(df.______)   # 결과: (8, 6)
```
- size
* shape
- count
- columns
> df.shape → (행 수, 열 수) 튜플을 반환한다.

5. 다음 코드의 실행 결과로 반환되는 자료형은? (2점)
```python
df[['이름', '수학', '영어']]
```
- Series
* DataFrame
- list
- dict
> 대괄호 두 겹 [[ ]]로 여러 열을 선택하면 DataFrame이 반환된다. (한 열만 [ ]이면 Series)

6. 다음 코드가 선택하는 행으로 옳은 것은? (2점)
```python
df.iloc[1:4]
```
- 1, 2, 3, 4번 행
* 1, 2, 3번 행
- 2, 3, 4번 행
- 0, 1, 2, 3번 행
> iloc 슬라이싱은 끝 번호를 포함하지 않는다(파이썬 일반 규칙).

7. df.loc[0:2]가 선택하는 행의 개수는? (loc 슬라이싱 규칙 적용) (2점)
- 2개
* 3개
- 1개
- 4개
> loc 슬라이싱은 끝 번호를 포함한다 → 0,1,2 = 3개.

8. df.describe()가 기본적으로 제공하는 통계량이 아닌 것은? (2점)
- 평균(mean)
- 표준편차(std)
* 최빈값(mode)
- 중앙값(50%)
> describe()는 count, mean, std, min, 25%, 50%, 75%, max를 제공한다. 최빈값은 미포함.

9. df.info()를 실행해서 확인할 수 없는 정보는? (2점)
- 열 이름
- 각 열의 데이터 타입
- 결측이 아닌 값(Non-Null)의 개수
* 각 열의 평균값
> info()는 열 이름·타입·Non-Null 개수를 요약한다. 평균은 describe()에서 확인.

10. 데이터의 앞에서부터 n줄만 미리 보고 싶을 때 사용하는 함수는? (2점)
```python
df.______(3)
```
- top
* head
- first
- tail
> head(n)은 앞에서 n줄, tail(n)은 뒤에서 n줄.

11. 값이 비어 있는 결측값을 pandas에서 표시하는 기호는? (2점)
- NULL
- None만 가능
* NaN
- 0
> NaN = Not a Number, 값이 없는 빈 칸.

12. 다음 코드의 결과가 의미하는 것으로 옳은 것은? (2점)
```python
print(df.isnull().sum())
```
- 전체 행의 개수
* 열별 결측값의 개수
- 중복된 행의 개수
- 각 열의 합계
> isnull()이 True/False를, sum()이 True(=1)를 세어 열별 결측 개수를 구한다.

13. 결측값이 포함된 행 자체를 삭제하는 함수는? (2점)
- fillna()
* dropna()
- drop_duplicates()
- isnull()
> dropna()는 결측값 있는 행을 삭제. fillna는 채우기, drop_duplicates는 중복 제거.

14. 결측값을 특정 값(예: 0)으로 채우려고 한다. 빈칸에 알맞은 함수는? (2점)
```python
df.______(0)
```
* fillna
- dropna
- replace_null
- setna
> df.fillna(0) — 0으로 채우기. 문자로 채우기도 가능.

15. 중복된 행을 찾아 제거하는 함수와, 중복 여부를 True/False로 확인하는 함수가 순서대로 바르게 짝지어진 것은? (2점)
- duplicated() / drop_duplicates()
* drop_duplicates() / duplicated()
- dropna() / isnull()
- unique() / value_counts()
> 제거 = drop_duplicates(), 확인 = duplicated().

16. df_clean = df_clean.reset_index(drop=True)에서 drop=True의 의미로 옳은 것은? (2점)
- 모든 행을 삭제한다
* 이전 인덱스를 새로운 컬럼으로 남기지 않고 버린다
- 인덱스를 내림차순으로 정렬한다
- 중복 인덱스만 삭제한다
> drop=True → 이전 인덱스를 컬럼으로 남기지 않고 버리고 0부터 새로 매긴다.

17. pandas에서 여러 조건을 동시에 거는 코드에서, AND와 OR을 의미하는 연산자가 바르게 짝지어진 것은? (2점)
- AND = and, OR = or
* AND = `&`, OR = `|`
- AND = `&&`, OR = `||`
- AND = `+`, OR = `-`
> pandas는 비트 연산자 &(AND), |(OR)를 사용하며, 각 조건은 괄호로 묶어야 한다.

18. 다음 코드의 동작을 가장 잘 설명한 것은? (2점)
```python
df['등급'] = df['평균'].apply(get_grade)
```
- '평균' 열을 삭제한다
* '평균' 열의 각 값에 get_grade 함수를 적용해 '등급' 열을 만든다
- '등급' 열의 평균을 계산한다
- 함수를 정의만 하고 실행하지는 않는다
> apply(함수)는 열의 각 값에 함수를 적용한 결과를 반환한다.

19. groupby()에 대한 설명으로 옳은 것은? (2점)
- 데이터를 무작위로 섞는다
* 특정 기준으로 데이터를 묶어 그룹별 통계를 내며, 엑셀의 피벗 테이블과 비슷하다
- 결측값을 자동으로 채워준다
- 두 표를 하나로 합친다
> groupby는 기준별로 묶어 집계하며, 엑셀 피벗 테이블과 유사하다.

20. 그룹별 행의 개수(그룹 크기)를 구하려고 한다. 빈칸에 알맞은 것은? (2점)
```python
df.groupby('학년').______()
```
- count
* size
- mean
- sum
> groupby('학년').size() → 그룹별 행 개수.

21. 두 값 사이에 관계(상관관계)가 있는지 점을 찍어 확인할 때 가장 적합한 그래프는? (2점)
- 막대 그래프
- 파이 차트
* 산점도(scatter)
- 박스 플롯
> scatter는 두 값의 관계(상관) 확인용이다.

22. 시간에 따른 변화나 흐름(추이)을 선으로 표현할 때 사용하는 함수는? (2점)
```python
plt.______(월, 기온, marker='o')
```
- bar
* plot
- pie
- scatter
> plt.plot()은 선 그래프(변화·추이)를 그린다.

23. 파이 차트에서 autopct='%1.1f%%' 옵션의 역할은? (2점)
- 조각의 색을 바꾼다
* 각 조각에 퍼센트(비율) 숫자를 자동으로 표시한다
- 특정 조각을 바깥으로 분리한다
- 범례를 표시한다
> autopct는 각 조각에 퍼센트를 자동 표시한다.

24. 파이 차트의 explode 옵션을 [0, 0.1, 0, 0]으로 지정했을 때의 효과는? (2점)
* 두 번째 조각을 살짝 바깥으로 분리해 강조한다
- 두 번째 조각을 삭제한다
- 전체 차트를 회전시킨다
- 두 번째 조각의 색을 진하게 한다
> explode 0.1 → 해당 조각을 살짝 바깥으로 분리해 강조한다(0이면 제자리).

25. 박스 플롯(boxplot)에서 상자 가운데 선이 의미하는 값은? (2점)
- 평균값
- 최댓값
* 중앙값(50%)
- 최솟값
> 상자 가운데 선 = 중앙값(50%), 상자 = Q1~Q3, 수염 = 전체 범위.

26. 그래프를 모두 그린 뒤 화면에 출력하기 위해 마지막에 반드시 실행해야 하는 명령은? (2점)
```python
plt.______()
```
- draw
- print
* show
- display
> plt.show()로 그래프를 화면에 출력한다.

27. CSV 파일을 읽어 DataFrame으로 만드는 함수는? (2점)
- pd.open_csv()
* pd.read_csv()
- pd.load_csv()
- pd.csv()
> CSV(쉼표로 구분된 값)를 DataFrame으로 읽는다.

28. 다음 출력에서 알 수 있는 합격 비율로 옳은 것은? (2점)
```python
print(df['합격여부'].value_counts())
#  1    180
# -1     20
```
- 50%
- 70%
* 90%
- 100%
> 180 / (180+20) × 100 = 90%.

29. 합격 90% : 불합격 10%처럼 한쪽 값으로 크게 치우친 데이터를 부르는 용어는? (2점)
- 정규 데이터
* 불균형 데이터(imbalanced data)
- 결측 데이터
- 중복 데이터
> imbalanced data — "전부 합격이라 찍어도 90%"의 함정에 주의.

30. 상관관계 히트맵 해석에 대한 설명으로 틀린 것은? (2점)
- 상관계수는 −1 ~ +1 사이의 값을 가진다
- 대각선은 항상 1.0이며 자기 자신과의 상관이라 의미가 없다
- 상관계수의 절댓값이 클수록 두 변수의 관계가 강하다
* 상관관계가 높으면 항상 한쪽이 다른 쪽의 원인(인과관계)이다
> 상관관계 ≠ 인과관계. 같이 움직인다고 한쪽이 원인인 것은 아니다.

31. 다음은 pandas를 pd라는 별명으로 불러오는 코드이다. 빈칸을 채우시오. (3점)
```python
import pandas [[as]] pd
```
> import pandas as pd

32. loc과 iloc의 차이를 한 문장으로 설명하시오. (2점)
= loc은 라벨(이름)로 선택하고 iloc은 위치(정수 번호)로 선택한다
= loc 이름 iloc 위치
= loc은 이름, iloc은 번호로 선택
> loc은 인덱스의 이름(라벨)으로, iloc은 위치(정수 번호, 0부터)로 선택한다. (loc은 슬라이싱 끝 포함, iloc은 끝 미포함)

33. 열별 결측값의 개수를 구하는 다음 코드의 빈칸을 채우시오. (3점)
```python
df.isnull().[[sum]]()
```
> df.isnull().sum() — 열별 결측값 개수.

34. df.shape의 출력이 (200, 12)일 때, 이 데이터의 행 수와 열 수를 차례로 쓰시오. (2점)
= 200행, 12열
= 행 200 열 12
= 200 12
> 첫 번째 값이 행 수(200), 두 번째 값이 열 수(12).

35. 센서 오류 등으로 값이 비어 있는 칸을 가리키는 용어와, pandas에서 이를 표시하는 기호(3글자 영문)를 쓰시오. (2점)
= 결측값, NaN
= 결측치, NaN
= 결측값 NaN
> 용어: 결측값(Missing Value), 기호: NaN(Not a Number).

36. value_counts() 함수가 하는 일을 한 문장으로 설명하시오. (2점)
= 한 열에서 값별로 개수(빈도)를 세어준다
= 값별 빈도를 세는 함수
= 각 값이 몇 번 나오는지 센다
> 한 열에서 같은 값이 각각 몇 번 나오는지(값별 빈도/개수)를 세어준다.

37. pandas에서 여러 조건을 결합할 때 사용하는 연산자이다. AND와 OR에 해당하는 기호를 차례로 쓰시오. (2점)
= &, |
= & |
= and→& or→|
> AND → &, OR → |. 각 조건은 반드시 괄호로 묶어야 한다.

38. 다음 코드는 무엇을 출력하는지 설명하시오. (2점)
```python
print(df['합격여부'].value_counts(normalize=True) * 100)
```
= 각 값이 차지하는 비율을 백분율(%)로 출력한다
= 값별 비율을 퍼센트로 출력
= 합격/불합격 비율을 % 로 출력
> '합격여부' 열의 각 값(1, -1)이 차지하는 비율을 백분율(%)로 출력한다(예: 90.0, 10.0).

39. 합격/불합격 그룹별로 각 센서값의 평균을 구하는 코드이다. 빈칸을 채우시오. (3점)
```python
df_clean.groupby('result')[numeric_cols].[[mean]]()
```
> groupby('result')[numeric_cols].mean() — 그룹별 평균.

40. df.describe() 결과에서 어떤 열의 count가 전체 행 수(200)보다 작게 나왔다. 이것이 의미하는 바를 쓰시오. (2점)
= 해당 열에 결측값이 있다
= 결측값이 존재한다
= 빈 칸(결측)이 있다는 뜻
> describe()는 결측을 제외하고 계산하므로, count가 전체 행 수보다 작으면 그 차이만큼 결측이 존재한다(예: count 195면 5개 결측).
